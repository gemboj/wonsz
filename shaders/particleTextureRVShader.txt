attribute vec2 aUVCoords;

uniform sampler2D uTexture;

void main(void) {
   vec4 pos = texture2D(uTexture, aUVCoords);

   
   //gl_Position = vec4((pos.xyz * 2.0) - 1.0, 1.0);
gl_Position = vec4((2.0 * pos.xyz) - 1.0, 1.0);
   gl_PointSize = 1.0;
}